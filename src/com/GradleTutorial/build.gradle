//apply plugin : 'java'
apply plugin : 'application' // It covers application plugin java

repositories{
    mavenCentral() // To download dependencies from mavenCentral
}

dependencies{ // Download testng dependencies
    testCompile group: 'org.testng', name: 'testng', version: '5.7'
}

/*jar{
    manifest{
        attributes 'Main-Class':'HelloWorld'
    }
}*/

mainClassName = 'GradleTutorial.HelloWorld'

task codeRun(type: JavaExec) {
    classpath sourceSets.main.runtimeClasspath
    main="HelloWorld"
}